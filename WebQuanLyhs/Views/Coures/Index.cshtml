@model IEnumerable<BusinessObject.Data.Course>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Coures_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category_Course)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Coures_name)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Category_Course.Category_description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Coures_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Coures_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Coures_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
